{
  "name": "example-server",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build &&node dist/entry.js",
    "dev": "ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "graphst": "file:../../packages/graphst",
    "http": "0.0.1-security"
  }
}
